# AI-Powered Gesture Volume Controller

This advanced AI project allows you to control your computer's system volume using hand gestures via your webcam. It utilizes MediaPipe for high-fidelity hand tracking and Pycaw for system audio interfacing.

## 🛠️ Requirements
- **Webcam:** For real-time video input.
- **Python Libraries:**
  - `opencv-python`: Image processing.
  - `mediapipe`: Google’s hand-tracking framework.
  - `pycaw`: Python Core Audio Windows Library.
  - `numpy`: For mathematical mapping.

## 📦 Installation
1. Install dependencies:
   ```bash
   pip install opencv-python mediapipe pycaw comtypes numpy
