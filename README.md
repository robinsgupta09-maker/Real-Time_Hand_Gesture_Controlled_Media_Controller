# Real-Time_Hand_Gesture_Controlled_Media_Controller
It uses a pre-trained Deep Learning model (MediaPipe) to track hand landmarks in real-time and map specific gestures (like the distance between your thumb and index finger) to system actions (like changing volume).
